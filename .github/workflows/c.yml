# fuh is currently only tested on Apple Silicon (macOS).
# The Linux CI from upstream feh has been disabled until
# Linux build support is added and tested.
#
# To re-enable, uncomment the workflow below and add:
#   sudo apt-get -y install libfreetype-dev libharfbuzz-dev libfontconfig-dev
# to the Install Dependencies step, and update the build command
# to pass the required CFLAGS/LDFLAGS.

# name: linux
# on: ...
